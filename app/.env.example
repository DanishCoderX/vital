# Points the app at your auth/sync backend (see the separate vital-backend project).
# For local dev, the default in src/lib/authApi.ts and syncApi.ts already works without setting this.
# For production builds, set this to your deployed Railway backend's /api URL, e.g.:
EXPO_PUBLIC_API_BASE_URL=https://your-vital-backend.up.railway.app/api
